coverage:
  status:
    project:
      default:
        target: 45%
        threshold: 2%
    patch:
      default:
        target: 70%
        informational: true  # informational until the team builds the habit

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: true
